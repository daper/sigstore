namespace: sigstore

resources:
  - namespace.yaml
  - build.yaml
  # - trillian.yaml

patches:
  - patch: |-
      apiVersion: kustomize.toolkit.fluxcd.io/v1
      kind: Kustomization
      metadata:
        name: not-used
      spec:
        sourceRef:
          kind: OCIRepository
          name: code
          namespace: flux-system
        targetNamespace: sigstore
        interval: 1m
        prune: true
    target:
      kind: Kustomization